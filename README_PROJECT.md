# kali — Spring Boot project

Simple Spring Boot project generated in this workspace.

Run locally with Maven:

```bash
mvn -e -DskipTests=false spring-boot:run
```

Build package:

```bash
mvn -DskipTests package
```

Application main class: `com.kali.KaliApplication`
